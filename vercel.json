{
  "buildCommand": "cd agripro-web && npm install && npm run build",
  "outputDirectory": "agripro-web/dist",
  "installCommand": "cd agripro-web && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
